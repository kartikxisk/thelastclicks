# Pin the public npm registry for this project. A project .npmrc overrides any
# global/user config, so a server whose ~/.npmrc points at GitHub Packages (or
# another private registry) can't hijack `npm ci` and 404 on public packages.
registry=https://registry.npmjs.org/
